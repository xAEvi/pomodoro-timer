import React from "react";
import tomato from '../images/tomato.png'

const Timer = props => {
    return (
        <div>
            <img src={tomato}></img>
        </div>
    );
}

export default Timer;